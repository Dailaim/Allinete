{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],

  "pipeline": {
    "build": {
      "dependsOn": ["^build", "^build.lib"],
      "outputs": ["dist/**"]
    },
    "lint": {},
    "dev": {
      "dependsOn": [],
      "cache": false,
      "persistent": true
    },
    "build.lib": {
      "outputs": ["dist/**", "lib/**"]
    }
  }
}
